{
  "name": "@p5/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch -r dotenv/config ./src/index.js",
    "start": "node --enable-source-maps -r dotenv/config ./src/index.js",
    "test": "vitest run",
    "prisma:generate": "prisma generate --schema ../../prisma/schema.prisma"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@fastify/jwt": "^10.0.0",
    "@fastify/rate-limit": "^10.3.0",
    "@prisma/client": "5.22.0",
    "dotenv": "^16.4.5",
    "fastify": "^5.1.0",
    "pino-pretty": "^11.2.2",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "prisma": "5.22.0",
    "supertest": "^7.0.0",
    "vitest": "^2.0.5"
  }
}
