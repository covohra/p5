{
  "name": "@p5/prisma",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev --name init",
    "seed": "node ./seed.cjs"
  },
  "dependencies": {
    "@prisma/client": "5.22.0",
    "dotenv": "16.4.5"
  },
  "devDependencies": {
    "prisma": "5.22.0"
  }
}