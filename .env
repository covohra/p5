# ==============================
# P5 API Service Environment
# ==============================

# --- App ---
NODE_ENV=development
PORT=3000

# --- Security ---
# Change this in your local .env if you want to test JWT:
JWT_SECRET=dev-super-secret-change-me
JWT_ACCESS_TTL=15m

# --- Rate limiting ---
RATE_LIMIT_MAX=100
RATE_LIMIT_TIME_WINDOW=1 minute

# --- CORS ---
# Comma-separated origins. For local dev:
CORS_ORIGINS=*

# --- Database (local Docker Compose: host 55432 -> container 5432) ---
DATABASE_URL=postgresql://p5:p5pass@127.0.0.1:55432/p5db?schema=public&sslmode=disable

# --- Redis (optional; later) ---
REDIS_URL=redis://127.0.0.1:6379/0

# --- SMTP (Mailpit for dev) ---
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=

# --- Build/Deploy metadata (set by CI if desired) ---
GIT_SHA=