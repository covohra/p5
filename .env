# App
NODE_ENV=development
PORT=3000

# Database (Docker maps host 55432 → container 5432)
DATABASE_URL=postgresql://p5:p5pass@127.0.0.1:55432/p5db?schema=public&sslmode=disable

# Redis (hosted by Docker)
REDIS_URL=redis://127.0.0.1:6379

# Mailpit (for local SMTP)
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=

# Fastify rate limit
RATE_LIMIT_MAX=100
RATE_LIMIT_TIME_WINDOW=1 minute