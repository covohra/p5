# --- App ---
NODE_ENV=development
PORT=3000

# --- Rate limit ---
RATE_LIMIT_MAX=100
RATE_LIMIT_TIME_WINDOW=1 minute

# --- CORS (comma-separated origins)
# e.g. https://your-frontend.com,https://staging.your-frontend.com
CORS_ORIGINS=*

# --- Database (local dev via Docker Compose) ---
DATABASE_URL=postgresql://p5:p5pass@127.0.0.1:55432/p5db?schema=public&sslmode=disable

# --- Redis (optional, not used yet) ---
REDIS_URL=redis://127.0.0.1:6379/0

# --- SMTP (Mailpit for dev; optional) ---
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=

# --- Build/Deploy metadata (optional; set by CI) ---
GIT_SHA=

# --- Auth (REQUIRED) ---
# Change in prod/staging via fly secrets
JWT_SECRET=dev-insecure-secret-change-me