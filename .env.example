# --- App ---
NODE_ENV=development
PORT=3000

# --- Rate limit ---
RATE_LIMIT_MAX=100
RATE_LIMIT_TIME_WINDOW=1 minute

# --- CORS (comma-separated origins). Example: https://your-frontend.com,https://staging.your-frontend.com
CORS_ORIGINS=*

# --- Local Postgres (Docker Compose) ---
DATABASE_URL=postgresql://p5:p5pass@127.0.0.1:55432/p5db?schema=public&sslmode=disable

# --- Redis (optional for later) ---
REDIS_URL=redis://127.0.0.1:6379/0

# --- SMTP (Mailpit for dev) ---
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=

# --- Build/Deploy metadata (injected by CI if desired) ---
GIT_SHA=
